// Alturas que un esqueleto tiene que reservar, con la cuenta escrita
// (docs/07 §7.3 y §9.2): un numero sin cuenta caduca al cambiar un subtitulo.

/** Titulo de tarjeta `Title fz="h5"`: 24 px de fuente × 1.2 de interlineado ≈ 29; el `Skeleton` cubre la caja de texto, 24. */
export const CARD_TITLE_HEIGHT = 24;
/** Cifras en `Text fz="caption" ff="mono"`: 12 px × 1.2 ≈ 14. */
export const CARD_META_HEIGHT = 14;
/** Titulo de pantalla en la cabecera (`h5` semibold): 24. */
export const SCREEN_TITLE_HEIGHT = 24;
/** Subtitulo de pantalla (`body3`): 12 px × 1.2 ≈ 14. */
export const SCREEN_SUBTITLE_HEIGHT = 14;
